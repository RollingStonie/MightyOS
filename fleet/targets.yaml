# fleet/targets.yaml — expected daily outputs, used by fleet_monitor.py for gap detection

mike:
  leads_per_day: 200
  max_errors_per_day: 10
  offline_alert_minutes: 15

mighty:
  emails_per_day: 50
  offline_alert_minutes: 30

brian:
  emails_per_day: 0
  offline_alert_minutes: 999

claire:
  offline_alert_minutes: 60

ashley:
  emails_per_day: 0
  offline_alert_minutes: 30

george:
  queries_per_day: 0          # TBD — pending verification + Slack channel setup (brand-knowledge Slack librarian)
  # 999 = alerts suppressed, same as brian: george has NO heartbeat mechanism at
  # all (george-gateway.service exists, george-heartbeat.service does not), so
  # george.json has been frozen at 2026-08-09 since it was hand-written once.
  # Nothing can clear this alert until george is actually onboarded — raising it
  # daily would just be noise. Restore to 30 when a heartbeat unit exists.
  offline_alert_minutes: 999
